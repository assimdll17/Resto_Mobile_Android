<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="ma.enset">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Resto"
        android:usesCleartextTraffic="true">
        <activity
            android:name=".PlatsByCategory"
            android:exported="false" />
        <activity
            android:name=".MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".AdminActivity"></activity>
        <activity android:name=".AddPlat"></activity>
        <activity android:name=".CommandeActivity"></activity>
        <activity android:name=".PlatDetailsActivity"></activity>
        <activity android:name=".ValidationActivity"></activity>
    </application>

</manifest>